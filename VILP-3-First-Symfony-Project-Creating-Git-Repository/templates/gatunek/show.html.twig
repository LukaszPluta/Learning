{% extends 'baselibrary.html.twig' %}

{% block title %}Gatunek{% endblock %}

{% block body %}
    <h1>Gatunek</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gatunek.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ gatunek.nazwa }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ gatunek.opis }}</td>
            </tr>
        </tbody>
    </table>

    <table class="table">
        {% for gatuneks in gatunek.ksiazkas %}
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gatuneks.id }}</td>
            </tr>
            <tr>
                <th>tytul</th>
                <td>{{ gatuneks.tytul }}</td>
            </tr>
            <tr>
                <td>autor</td>
                <td><a href="{{ path('autor_show', {'id': gatuneks.autor.id}) }}">{{ gatuneks.autor.imie }} {{ gatuneks.autor.nazwisko }}</a></td>
            </tr>
            <tr>
                <td>rok wydania</td>
                <td>{{ gatuneks.rokWydania }}</td>
            </tr>
            <tr>
                <td></td>
                <td><a href="{{ path('ksiazka_show', {'id': gatuneks.id}) }}">show</a></td>
            </tr>
            </tbody>
        {% endfor %}
    </table>

    <a href="{{ path('gatunek_index') }}">back to list</a>

    <a href="{{ path('gatunek_edit', {'id': gatunek.id}) }}">edit</a>

    {{ include('gatunek/_delete_form.html.twig') }}
{% endblock %}
